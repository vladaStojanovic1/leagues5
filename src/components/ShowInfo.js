import React, { useState } from 'react'

export const ShowInfo = ({ showInfo, match }) => {

    console.log(match);


    return (
        <div style={{ color: 'white' }}>

            {/* {showInfo ? <h1>Infooooo...</h1> : null}
             */}
            <h1>SHow info</h1>
        </div>
    )
}
